{block content}
    <div class="row">
        <div class="col-lg-12">
            <h1>Návrhy vylepšení</h1>
            <table class="table table-striped table-bordered table-hover">
                <thead>
                <tr>
                    <th>#</th>
                    <th>Název</th>
                    <th>Uživatel</th>
                    <th>Čas</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {foreach $data as $item}
                    <tr>
                        <td>{$item->id}</td>
                        <td>{$item->name}</td>
                        <td>{$item->user|UserName}</td>
                        <td>{$item->time|date:'%H:%m - %d.%m.%Y'}</td>
                        <td class="text-center">
                            <a n:href="System:showUpdate $item->id" class="btn btn-primary"><i class="fa fa-eye fa fw"></i></a>
                        </td>
                    </tr>
                {/foreach}
                </tbody>
            </table>
        </div>
    </div>
{/block}